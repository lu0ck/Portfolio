# Portfolio

Professional portfolio built with React, Vite, and Tailwind CSS.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run development server:
   ```bash
   npm run dev
   ```

## GitHub Integration
This portfolio fetches data directly from the GitHub API for the user `lu0ck`.

## Deployment
The app is configured for deployment on Cloud Run.
